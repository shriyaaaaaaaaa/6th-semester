/*using System;

class Program
{
    static void Main()
    {
        // Prompt the user to enter the distance in kilometers
        Console.Write("Enter the distance between two cities (in kilometers): ");
        double distanceInKm = Convert.ToDouble(Console.ReadLine());

        // Convert the distance to different units
        double distanceInMeters = distanceInKm * 1000;          // 1 km = 1000 meters
        double distanceInFeet = distanceInKm * 3280.84;         // 1 km = 3280.84 feet
        double distanceInInches = distanceInKm * 39370.08;      // 1 km = 39370.08 inches
        double distanceInCentimeters = distanceInKm * 100000;   // 1 km = 100000 cm

        // Display the results
        Console.WriteLine($"Distance in meters: {distanceInMeters} m");
        Console.WriteLine($"Distance in feet: {distanceInFeet} ft");
        Console.WriteLine($"Distance in inches: {distanceInInches} in");
        Console.WriteLine($"Distance in centimeters: {distanceInCentimeters} cm");
    }
}
*/