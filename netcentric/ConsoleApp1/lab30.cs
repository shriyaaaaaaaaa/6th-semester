/*using System;

class Program
{
    static void Main()
    {
        Console.Write("Enter cost price: ");
        double costPrice = double.Parse(Console.ReadLine());

        Console.Write("Enter selling price: ");
        double sellingPrice = double.Parse(Console.ReadLine());

        if (sellingPrice > costPrice)
        {
            double profit = sellingPrice - costPrice;
            Console.WriteLine($"The seller made a profit of Rs. {profit}");
        }
        else if (sellingPrice < costPrice)
        {
            double loss = costPrice - sellingPrice;
            Console.WriteLine($"The seller incurred a loss of Rs. {loss}");
        }
        else
        {
            Console.WriteLine("No profit, no loss.");
        }
    }
}
*/