/*
 * using System;

class Program
{
    static void Main()
    {
        // Input number of males and females
        Console.Write("Enter the number of males: ");
        int males = Convert.ToInt32(Console.ReadLine());

        Console.Write("Enter the number of females: ");
        int females = Convert.ToInt32(Console.ReadLine());

        // Calculate the ratio
        if (females != 0) // Avoid division by zero
        {
            float ratio = (float)males / females;
            Console.WriteLine($"The ratio of males to females is: {ratio}");
        }
        else
        {
            Console.WriteLine("Error: The number of females cannot be zero.");
        }
    }
}
*/