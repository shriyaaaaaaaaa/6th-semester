/*using System;

class Program
{
    static void Main()
    {
        // Prompt the user to enter the radius of the sphere
        Console.Write("Enter the radius of the sphere: ");
        double radius = Convert.ToDouble(Console.ReadLine());

        // Calculate the volume and surface area
        double volume = (4.0 / 3.0) * Math.PI * Math.Pow(radius, 3); // Volume = (4/3)πr³
        double area = 4 * Math.PI * Math.Pow(radius, 2);            // Area = 4πr²

        // Display the results
        Console.WriteLine($"The volume of the sphere is: {volume}");
        Console.WriteLine($"The surface area of the sphere is: {area}");
    }
}
*/