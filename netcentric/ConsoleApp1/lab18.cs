/*
 using System;

class Program
{
    static void Main()
    {
        // Input the number of days
        Console.Write("Enter the number of days: ");
        int totalDays = Convert.ToInt32(Console.ReadLine());

        // Convert days to months and remaining days
        int months = totalDays / 30;
        int remainingDays = totalDays % 30;

        // Display the result
        Console.WriteLine($"{totalDays} days is approximately {months} month(s) and {remainingDays} day(s).");
    }
}
*/