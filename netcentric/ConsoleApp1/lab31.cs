/*using System;

class Program
{
    static void Main()
    {
        Console.Write("Enter the current year: ");
        int currentYear = int.Parse(Console.ReadLine());

        Console.Write("Enter the year the employee joined: ");
        int joiningYear = int.Parse(Console.ReadLine());

        int yearsOfService = currentYear - joiningYear;

        if (yearsOfService > 3)
        {
            Console.WriteLine("The employee is eligible for a bonus of Rs. 2500/-.");
        }
        else
        {
            Console.WriteLine("The employee is not eligible for a bonus.");
        }
    }
}
*/