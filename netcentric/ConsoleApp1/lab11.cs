/*using System;

class Program
{
    static void Main()
    {
        // Given total amount after 10% discount
        double totalAfterDiscount = 1000.5;

        // The discount percentage
        double discountPercentage = 10;

        // Calculate the amount before discount
        double amountBeforeDiscount = totalAfterDiscount / (1 - discountPercentage / 100);

        // Calculate the discount
        double discountAmount = amountBeforeDiscount - totalAfterDiscount;

        // Display the results
        Console.WriteLine($"Amount before discount: Rs. {amountBeforeDiscount}");
        Console.WriteLine($"Discount amount: Rs. {discountAmount}");
    }
}
*/